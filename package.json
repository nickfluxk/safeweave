{
  "name": "safeweave",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/common",
    "packages/gateway",
    "packages/scanners/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "npm run clean --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
